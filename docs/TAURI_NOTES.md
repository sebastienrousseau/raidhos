# Tauri Notes

Tauri builds require OS-specific system dependencies.

- Linux: WebKit2GTK, libgtk-3, and related dev packages.
- macOS: Xcode command line tools.
- Windows: WebView2 runtime and MSVC build tools.

We will document exact package lists per OS once the UI matures.
