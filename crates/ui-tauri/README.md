# RaidhOS UI (Tauri)

The Tauri app lives in `src-tauri/`. The frontend is a static HTML scaffold in `frontend/`.

## Local dev (later)

You will need the Tauri CLI and OS-specific dependencies. For now this is a scaffold.
